# SummonYourWill Mobile React App

This is a standalone React version of the game UI designed for phones. It reuses the same game logic from the desktop build without including any Electron code.

## Development

```
npm install
npm run dev
```

The app can load JSON save files exported from the desktop application via the **Import** button.
